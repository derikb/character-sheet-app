# Security Fix for derikb/character-sheet-app

Addresses 2 security finding(s).

## Changes Required

### Withdrawn Advisory: esbuild: Missing binary integrity verification in Deno module enables remote code execution via NPM_CONFIG_REGISTRY

- **File:** `package.json`
- **Severity:** HIGH
- **CWE:** [CWE-426](https://cwe.mitre.org/data/definitions/426.html)
- **Fix:** Update `esbuild` to version `0.28.1` or later.

Run:
  npm update esbuild  # For npm
  pip install --upgrade esbuild  # For Python
  go get esbuild@latest  # For Go

### esbuild allows arbitrary file read when running the development server on Windows

- **File:** `package.json`
- **Severity:** LOW
- **CWE:** [CWE-22](https://cwe.mitre.org/data/definitions/22.html)
- **Fix:** Update `esbuild` to version `0.28.1` or later.

Run:
  npm update esbuild  # For npm
  pip install --upgrade esbuild  # For Python
  go get esbuild@latest  # For Go

---

Generated by [mcontributor](https://github.com/mcontributor) security scanner.